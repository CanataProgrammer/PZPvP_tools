了解です。今後「ランダム配置だけのMod」から、**Minecraft 1.20.1 Forge向けの汎用PvPツールセット**へ発展させる前提で、要件書を「CZPvP_tools」として整理するとよいです。

# CZPvP_tools 要件書

## 1. Mod概要

### 1.1 Mod名称

**CZPvP_tools**

表示名：

**CZPvP Tools**

### 1.2 コンセプト

Minecraft 1.20.1 Forgeを対象とした、**FPSゲーム・PvPゲームのゲームシステムをMinecraft上で構築するための汎用ツールセットMod**。

現時点では「安全なランダムスポーン・ランダム配置」を主要機能として実装する。

ただし、今後以下のようなPvP機能を段階的に追加できるよう、**拡張性を重視した設計**とする。

* ゲーム開始・終了
* ラウンド管理
* キル・デスカウンター
* K/D表示
* スコアボード
* チーム管理
* ロードアウト選択
* リスポーン
* スポーン保護
* ゲームモード
* 勝利条件
* タイマー
* キルログ
* HUD
* 観戦
* マップ・戦場管理

---

# 2. 基本方針

CZPvP_toolsは、特定のPvPゲームだけを実装するModではなく、

> **「MinecraftをPvPゲームのゲームエンジンとして利用するためのツールセット」**

を目指す。

そのため、各機能を独立したモジュールとして実装する。

```text
CZPvP_tools
│
├── Game System
├── Player System
├── Team System
├── Spawn System
├── Counter System
├── Loadout System
├── Scoreboard System
├── Round System
├── HUD System
└── Map System
```

現在実装するのは **Spawn System** のみとする。

---

# 3. 現在実装する機能

## 3.1 Random Spawn System

指定された戦場範囲内から、安全なランダムスポーン地点を検索してプレイヤーを配置する。

### 必須条件

* 範囲外に出ない
* 水を避ける
* 溶岩を避ける
* ブロック内部を避ける
* 空中を避ける
* プレイヤー同士が近すぎないようにする
* チームごとのスポーン範囲に対応
* 全体ランダム配置に対応
* ラウンド開始時に実行可能
* リスポーン時に実行可能

---

# 4. コマンド体系

今後の機能追加を考慮し、コマンドのルートをすべて

```text
/czpvp
```

に統一する。

## 現在実装

### ゲーム開始

```text
/czpvp start
```

ゲームを開始する。

現段階では、ゲーム開始時に参加プレイヤーをランダム配置する。

---

### ゲーム終了

```text
/czpvp stop
```

ゲームを終了する。

現段階では、ゲーム状態を終了状態に戻す。

---

### ランダム配置

```text
/czpvp randomtp
```

現在の参加プレイヤーをランダム配置する。

---

### プレイヤー指定

```text
/czpvp randomtp <player>
```

指定プレイヤーのみランダム配置する。

---

# 5. ゲーム状態

将来的なゲームシステム追加を考慮して、ゲームの状態を管理する。

```text
WAITING
   ↓
STARTING
   ↓
PLAYING
   ↓
ENDING
   ↓
WAITING
```

### WAITING

ゲーム開始待機中。

### STARTING

ゲーム開始処理中。

### PLAYING

PvPゲーム進行中。

### ENDING

ゲーム終了処理中。

---

# 6. プレイヤー管理

将来的な機能追加に備えて、CZPvP_tools独自の「参加プレイヤー」を管理する。

例えば、

```text
CZPvP参加者
├── PlayerA
├── PlayerB
├── PlayerC
└── PlayerD
```

Spectatorなどのゲーム非参加者は別扱いとする。

将来的には、

```text
/czpvp join
/czpvp leave
/czpvp spectator
```

などを追加できる設計にする。

---

# 7. スポーンシステム

## 7.1 戦場範囲

設定された範囲内からスポーン地点を検索する。

```text
minX
maxX
minY
maxY
minZ
maxZ
```

---

## 7.2 ランダム配置方式

### RANDOM

戦場全体からランダム配置。

```toml
spawnMode = "RANDOM"
```

### TEAM

チームごとに指定された範囲からランダム配置。

```toml
spawnMode = "TEAM"
```

---

# 8. 安全地点判定

スポーン地点は以下をすべて満たす必要がある。

```text
範囲内
 ↓
足場あり
 ↓
プレイヤーが立てる
 ↓
水ではない
 ↓
溶岩ではない
 ↓
ブロックに埋まらない
 ↓
空中ではない
 ↓
他プレイヤーとの距離OK
 ↓
スポーン地点決定
```

---

# 9. プレイヤー間距離

デフォルト：

```toml
minimumPlayerDistance = 10
```

敵プレイヤーについては将来的に別の距離を設定できる構造にする。

```toml
minimumEnemyDistance = 15
```

---

# 10. リスポーン

CZPvPゲーム中にプレイヤーが死亡した場合、通常のワールドスポーンではなく、CZPvP_toolsのSpawn Systemを使用する。

```text
死亡
 ↓
Respawn
 ↓
CZPvP参加中？
 ↓ YES
SpawnLocationFinder
 ↓
安全地点検索
 ↓
ランダム配置
```

---

# 11. 将来追加する機能

ここからが**CZPvP_toolsの重要な部分**。

今後のアップデートで、以下のシステムを追加できる設計にする。

---

## 11.1 Counter System

PvPで使用する各種カウンター。

例：

```text
KILLS
DEATHS
ASSISTS
SCORE
DAMAGE
HEADSHOTS
```

プレイヤーごとに統計情報を管理する。

例えば、

```text
PlayerA
KILLS  : 12
DEATHS : 4
SCORE  : 1500
```

---

# 12. K/Dシステム

KILLSとDEATHSからK/Dを計算する。

```text
K/D = KILLS ÷ DEATHS
```

死亡数が0の場合は、

```text
K/D = KILLS
```

など、0除算を避ける仕様にする。

---

# 13. Scoreboard System

FPSゲームのような右側スコアボードを実装可能にする。

将来的な表示例：

```text
      CZPvP
 ─────────────
 KILLS      12
 DEATHS      4
 K/D      3.00
 ─────────────
 SCORE     1500
```

チーム戦では、

```text
       CZPvP
 ─────────────
 RED       120
 BLUE       98
 ─────────────
 ROUND     3/10
```

などを表示可能にする。

---

# 14. Game Start System

将来的に `/czpvp start` を拡張する。

```text
/czpvp start
```

↓

```text
Countdown
5
4
3
2
1
GO!
```

↓

```text
プレイヤー配置
↓
ロードアウト配布
↓
ラウンド開始
```

というFPSゲーム風のゲーム開始処理を可能にする。

---

# 15. Loadout System

将来的にプレイヤーがゲーム開始前に装備を選択できるようにする。

例えば、

```text
/czpvp loadout
```

でGUIを開く。

```text
┌──────────────────────────┐
│       SELECT LOADOUT     │
│                          │
│  [ASSAULT]  [SNIPER]    │
│                          │
│  [SMG]      [SHOTGUN]   │
│                          │
│  [CUSTOM]               │
└──────────────────────────┘
```

ロードアウトには、

* 武器
* 防具
* アイテム
* 弾薬
* 特殊アイテム

などを登録できるようにする。

---

# 16. Team System

チームを管理する。

例：

```text
RED
BLUE
GREEN
YELLOW
```

将来的には、

```text
/czpvp team join red
/czpvp team join blue
```

などを追加する。

チームごとに、

* スポーン地点
* 色
* スコア
* ロードアウト
* 勝利条件

などを設定可能にする。

---

# 17. Round System

ラウンド制ゲームをサポートする。

例えば、

```text
ROUND 1
↓
ROUND 2
↓
ROUND 3
↓
...
↓
ROUND 10
```

設定例：

```toml
rounds = 10
roundTime = 300
```

---

# 18. 勝利条件

将来的にゲームモードごとの勝利条件を実装する。

例：

### Team Deathmatch

```text
先に100キル
```

### Search & Destroy

```text
規定ラウンド勝利
```

### Free For All

```text
最もスコアが高いプレイヤー
```

### Domination

```text
目標地点の占領率
```

---

# 19. HUD System

将来的に画面上へPvP情報を表示する。

例：

```text
KILLS: 12
DEATHS: 4

AMMO: 24 / 120

ROUND: 3

TIME: 02:41
```

---

# 20. Kill Feed

FPSゲームのようなキルログを追加できるようにする。

```text
PlayerA  [AK-47]  PlayerB
PlayerC  [M4A1]   PlayerD
```

などの表示を可能にする。

---

# 21. Map System

将来的には複数のPvPマップを管理できるようにする。

```text
/czpvp map list
/czpvp map load <map>
/czpvp map save <map>
```

マップごとに、

* 戦場範囲
* チームスポーン
* ランダムスポーン範囲
* 勝利条件
* ロードアウト
* ゲームモード

を設定できるようにする。

---

# 22. 設定ファイル

初期段階では最低限、

```toml
[spawn]

spawnMode = "RANDOM"

minimumPlayerDistance = 10

minimumEnemyDistance = 15

maxAttempts = 100

avoidWater = true

avoidLava = true

avoidBlocks = true

avoidAir = true


[arena]

minX = -500
maxX = 500

minY = 60
maxY = 100

minZ = -300
maxZ = 300
```

を実装する。

将来的に、

```text
[game]
[round]
[team]
[loadout]
[scoreboard]
[hud]
[respawn]
[map]
```

などを追加する。

---

# 23. 推奨プログラム構造

拡張性を最重要視し、最初から機能を分離する。

```text
com.example.czpvp_tools
│
├── CZPvPTools.java
│
├── command
│   └── CZPvPCommand.java
│
├── game
│   ├── GameManager.java
│   ├── GameState.java
│   └── RoundManager.java
│
├── player
│   └── PvPPlayerManager.java
│
├── spawn
│   ├── RandomSpawnManager.java
│   ├── SpawnLocationFinder.java
│   ├── SafeLocationValidator.java
│   └── SpawnConfig.java
│
├── team
│   └── TeamManager.java
│
├── counter
│   └── CounterManager.java
│
├── scoreboard
│   └── ScoreboardManager.java
│
├── loadout
│   └── LoadoutManager.java
│
├── hud
│   └── HudManager.java
│
├── map
│   └── MapManager.java
│
└── config
    └── CZPvPConfig.java
```

**現時点では実装不要なクラスについても、将来追加しやすい構造を意識する。**

---

# 24. 開発フェーズ

### Phase 1 — 現在

**Random Spawn**

* `/czpvp start`
* `/czpvp stop`
* `/czpvp randomtp`
* 戦場範囲
* 安全地点検索
* プレイヤー間距離
* RANDOM / TEAM配置
* リスポーン時配置

### Phase 2

**Game System**

* ゲーム参加
* ゲーム状態
* カウントダウン
* ラウンド

### Phase 3

**Counter / Scoreboard**

* Kills
* Deaths
* K/D
* Score
* FPS風スコアボード

### Phase 4

**Team System**

* チーム
* チームスポーン
* チームスコア
* チーム勝利条件

### Phase 5

**Loadout System**

* ロードアウト
* GUI
* 武器・アイテム配布
* カスタムロードアウト

### Phase 6

**Advanced PvP System**

* Kill Feed
* HUD
* リスポーン保護
* ゲームモード
* 勝利条件
* マップ管理

---

## 25. 最終的な位置付け

**CZPvP_toolsは「1つのPvPゲーム」ではなく、PvPゲームを作るための基盤Modとする。**

最終的には、

```text
                 CZPvP_tools
                      │
       ┌──────────────┼──────────────┐
       ↓              ↓              ↓
    Game System    Player System   Map System
       │              │              │
       ↓              ↓              ↓
    Round          Loadout         Spawn
       │              │              │
       └──────────────┼──────────────┘
                      ↓
               Counter System
                      ↓
              Scoreboard / HUD
                      ↓
                PvP Game
```

という構造を目指します。

---

# 26. 実装状況の追記（現状ログ）

本セクションは、当初「Phase 1（Random Spawn）のみ実装」としていた本要件書に対し、実際に実装が進んだ内容・企画書に記載のなかった機能を反映するための追記です。以降、変更・追加を行うたびに本セクションへ追記していきます。

## 26.1 現在実装済みのコマンド体系

```text
/czpvp join
/czpvp leave
/czpvp list

/czpvp start
/czpvp stop

/czpvp randomtp
/czpvp randomtp <player>

/czpvp team create <name>
/czpvp team delete <name>
/czpvp team join <name>
/czpvp team leave
/czpvp team list
/czpvp team gui
/czpvp team leader <name> <player>
/czpvp team leader <name> clear
/czpvp team vice <name> <player>
/czpvp team vice <name> clear

/czpvp gamesetting arena set <pos1> <pos2>
/czpvp gamesetting arena info
/czpvp gamesetting spawnmode random
/czpvp gamesetting spawnmode team
/czpvp gamesetting respawnmode random
/czpvp gamesetting respawnmode fixed <pos>
/czpvp gamesetting respawnmode info
/czpvp gamesetting teamarea <name> <pos1> <pos2>
/czpvp gamesetting time info
/czpvp gamesetting time <minutes>
/czpvp gamesetting endtp true <pos>
/czpvp gamesetting endtp false
/czpvp gamesetting endtp info

/czpvp scoreboard show
/czpvp scoreboard csv
/czpvp scoreboard reset
```

## 26.2 Game System（Phase 2 実装済み）

* `GameState`: `WAITING` → `STARTING` → `PLAYING` → `ENDING` の状態遷移を`GameManager`で管理
* `/czpvp start` / `/czpvp stop` でゲーム開始・終了
* 参加プレイヤー管理: `/czpvp join`, `leave`, `list`（`PvPPlayerManager`）
* ゲームタイマー: `/czpvp gamesetting time <minutes>` で制限時間を設定し、経過で自動終了（`GameTimerHandler`）
* ゲーム終了時テレポート: `/czpvp gamesetting endtp` で終了時に指定地点へ一斉テレポート可能（`EndTeleportManager`）
* ゲーム開始/終了のアナウンス（`GameAnnouncer`）、中途参加者への状態同期

## 26.3 Counter / Scoreboard System（Phase 3 実装済み）

* Kills / Deaths / DamageDealt / DamageTaken をプレイヤーごとに集計（`CounterManager`, `PlayerStats`, `CombatEventHandler`）
* スコア計算・K/D計算は0除算を回避する仕様
* `/czpvp scoreboard show` でクライアントにスコアボードGUIを表示（`ScoreboardDataPacket` → `ClientScoreboardHandler` / `ScoreboardScreen`）
* `/czpvp scoreboard csv` でCSV出力（`ScoreboardCsvExporter`）、`/czpvp scoreboard reset` でリセット
* キルリワード機能（企画書未記載の追加機能）: キル時に指定アイテムを付与。`[killReward]` セクションで有効/無効・アイテム・個数を設定可能

## 26.4 Team System（Phase 4 実装済み）

* `/czpvp team create/delete/join/leave/list/gui`
* `/czpvp gamesetting teamarea <name> <pos1> <pos2>` でチームごとのスポーン範囲を設定
* `/czpvp gamesetting spawnmode random/team` で全体ランダム配置とチーム別配置を切り替え
* チーム管理GUI（`TeamGuiScreen`）: チーム一覧表示、参加/離脱、（op限定）作成/削除

### 26.4.1 隊長・副隊長システム（新規追加）

チーム内から隊長・副隊長を指定できる機能を追加。

* `Team` に `leaderId` / `viceLeaderId` を追加
* コマンド（op限定）:

```text
/czpvp team leader <name> <player>   隊長を設定
/czpvp team leader <name> clear      隊長を解除
/czpvp team vice <name> <player>     副隊長を設定
/czpvp team vice <name> clear        副隊長を解除
```

* 隊長・副隊長は同一人物に重複設定不可（片方に設定すると、もう一方の役職だった場合は自動解除）
* 対象プレイヤーがそのチームのメンバーでない場合は設定失敗
* チーム離脱時、当人が隊長・副隊長であれば役職を自動解除
* `/czpvp team list` に隊長・副隊長名を表示（未設定は「(未設定)」、オフラインは「(オフライン)」と表示）

### 26.4.2 隊長基準リスポーン（新規追加）

配置モードが `TEAM` の場合、リスポーン地点の決定方法を拡張した。

```text
配置モード = TEAM かつ 死亡プレイヤーがチーム所属
 ↓
隊長（オンライン）の座標
 ↓ 隊長が居ない場合
副隊長（オンライン）の座標
 ↓ 副隊長も居ない場合
ランダムなオンラインチームメイトの座標
 ↓ チームメイトが全員オフラインの場合
従来の安全地点ランダム検索（チーム範囲 or 全体範囲）にフォールバック
```

* 実装: `RandomSpawnManager.respawnRandom()` / `resolveRespawnAnchor()`
* 配置モードが `RANDOM` の場合、またはプレイヤーが未所属チームの場合は従来どおりの安全地点ランダム検索を使用
* `RespawnManager.RespawnMode.FIXED`（固定リスポーン）が設定されている場合はこの機能より優先される

## 26.5 リスポーンモード（企画書未記載の追加機能）

* `/czpvp gamesetting respawnmode random/fixed/info`
* `RANDOM`: 上記の安全地点ランダム検索（`TEAM`配置モード時は26.4.2の隊長基準リスポーンが優先）
* `FIXED`: 管理者が指定した固定座標に常にリスポーン

## 26.6 バトルエリアの杖（新規追加）

戦場範囲（Spawn Systemが参照するグローバルエリア）を、コマンドではなくアイテムで直感的に設定できる管理者用ツールを追加した。

* アイテムID: `czpvp_tools:battle_area_wand`
* 見た目: 杖の先端に赤い宝石が付いたテクスチャ（`textures/item/battle_area_wand.png`）
* 入手方法: クリエイティブタブ、または `/give` コマンドのみ（クラフトレシピなし）
* 使用権限: 権限レベル2（op）以上のプレイヤーのみ使用可能。それ以外が使用した場合は「このアイテムを使用する権限がありません。」と表示し何も起きない

### 操作方法

```text
通常右クリック（1回目・ブロック対象）
 ↓
始点(P1)を記録
 ↓
通常右クリック（2回目・ブロック対象）
 ↓
終点(P2)を記録し、設定中の範囲形状でエリアを確定
 ↓
ArenaManagerのグローバルエリアに反映（/czpvp gamesetting arena set と同じ場所）
 ↓
（ワールドボーダー同期がONの場合）ワールドボーダーをエリアに合わせて更新
```

* シフト右クリック: 選択操作を行わず、設定画面（GUI）を開く

### 設定画面（シフト右クリックで表示）

* **範囲形状**: 「長方形」または「正方形」を選択
  * 長方形: P1・P2の対角2点からそのままの直方体を作成（`/czpvp gamesetting arena set` と同じ計算）
  * 正方形: P1を基点とし、P1→P2のX/Z差の大きい方を一辺の長さとしてX/Z正方形に補正（Yの範囲はP1・P2そのまま）
* **ワールドボーダー同期**: ON/OFFの切り替え
  * ONの場合、エリア確定時にワールドの`WorldBorder`の中心・サイズを戦場範囲に合わせて自動設定する
  * バニラのワールドボーダーは常に正方形（X/Z共通の1辺サイズ）のため、長方形を選んでいる場合はその範囲を包含する正方形として反映される

### 実装ファイル

* `item/BattleAreaWandItem.java`（アイテム本体・右クリック処理）
* `item/BattleAreaSettings.java`（範囲形状・ワールドボーダー同期のグローバル設定）
* `item/BattleAreaSelectionManager.java`（プレイヤーごとの始点の一時保持）
* `network/BattleAreaSettingsPacket.java`（S→C: 設定画面を開く）
* `network/BattleAreaActionPacket.java`（C→S: 設定画面での操作を反映）
* `client/BattleAreaSettingsScreen.java`, `client/ClientBattleAreaHandler.java`（設定画面GUI）

## 26.7 未実装（Phase 5, 6は今後）

* Loadout System（装備選択GUI）
* HUD System（キル数以外の常時表示、弾薬等）
* Kill Feed
* Round System（ラウンド制）
* Map System（複数マップの保存/読込）
* 勝利条件・ゲームモード別ルール
* 観戦モード（Spectator）
* チームGUIからの隊長・副隊長設定（現状はコマンドのみ対応）
